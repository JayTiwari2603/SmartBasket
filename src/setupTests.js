// This file runs before your test suite and sets up the testing environment.
// Create React App includes this file by default so you can install helpers
// that make testing with Jest + React Testing Library easier.
//
// We import `@testing-library/jest-dom` which adds custom matchers like:
// - toBeInTheDocument()
// - toHaveTextContent()
// - toHaveAttribute()
// These helpers make assertions in tests more readable and expressive.
// Example in a test:
// expect(screen.getByText('Hello')).toBeInTheDocument();
import '@testing-library/jest-dom';
